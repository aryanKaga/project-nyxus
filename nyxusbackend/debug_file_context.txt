File: bert_topic_analysis.py
Length of file contents: 5362
Length of context returned: 3485
LLM Response:
{
  "context": "# Purpose\nThis script performs topic modeling on Amazon product reviews using the BERTopic library. It loads review text, preprocesses it, trains BERTopic models using custom UMAP and HDBSCAN parameters, evaluates them using a Gensim CoherenceModel (c_v), tunes hyperparameters via grid search, and saves the best-performing model.\n\n# Components\n- `review_stopwords`: List of custom domain-specific stopwords to be removed during vectorization.\n- `return_preprocessed_data()`: Reads the CSV dataset, cleans missing/empty values, and sets up a custom `CountVectorizer` with n-grams and stop word filters.\n- `get_topic_words()`: Extracts top words for each discovered topic, excluding outliers.\n- `preprocess_for_coherence()`: Performs tokenization, lemmatization, and builds a Gensim dictionary and corpus specifically for computing topic coherence.\n- `train_bert_topic()`: Initializes UMAP, HDBSCAN, and BERTopic models, fits them to the documents, and calculates the c_v coherence score.\n- `train_bert_model()`: Runs a parameter grid search over UMAP and HDBSCAN parameters to find and save the model with the highest coherence score.\n- `main()`: Entry point executing the training pipeline.\n\n# Imports\n- `pandas`, `itertools`, `matplotlib.pyplot`\n- `gensim.corpora.Dictionary`, `gensim.models.CoherenceModel`\n- `nltk`, `nltk.corpus.stopwords`, `nltk.stem.WordNetLemmatizer`, `nltk.tokenize.word_tokenize`\n- `re`\n- `bertopic.BERTopic`\n- `sklearn.feature_extraction.text.CountVectorizer`, `sklearn.feature_extraction.text.ENGLISH_STOP_WORDS`\n- `umap.UMAP`\n- `hdbscan.HDBSCAN`\n\n# Exports\n- Functions are internal (not explicitly exported via `__all__`), but main operational entry points include `train_bert_model` and `main`.\n\n# Relationships\n- Depends on external Python packages (pandas, gensim, nltk, bertopic, sklearn, umap, hdbscan).\n- Reads an external CSV file from a hardcoded path (`E:\\myProgramme\\nlp\\bert_topic_modeling\\Amazon_Reviews.csv`).\n\n# Execution Flow\n1. `main()` calls `train_bert_model()`.\n2. `train_bert_model()` calls `return_preprocessed_data()` to fetch document lists and the count vectorizer.\n3. It loops through hyperparameter configurations (`itertools.product`).\n4. For each iteration, `train_bert_topic()` is called to build and fit the UMAP, HDBSCAN, and BERTopic pipelines.\n5. Topic coherence is evaluated using `preprocess_for_coherence()` and Gensim's `CoherenceModel`.\n6. The model yielding the highest coherence score is saved to disk via `best_model.save()`.\n\n# State Changes\n- Reads and modifies Pandas DataFrames from a CSV source.\n- Creates and saves a serialized BERTopic model directory (`best_bert_topic_model`) on the local filesystem.\n\n# Risks\n- Uses hardcoded absolute Windows file paths (`E:\\myProgramme\\nlp\\bert_topic_modeling\\Amazon_Reviews.csv`), which will fail on other machines or operating systems.\n- Repeats CSV file loading operations inside multiple helper functions (`return_preprocessed_data` and `preprocess_for_coherence`), causing redundant I/O.\n\n# Important Symbols\n- `review_stopwords`: List (Constant) - Custom stopwords for filtering noise in review text.\n- `return_preprocessed_data`: Function - Loads data and returns preprocessed review documents and a configured CountVectorizer.\n- `train_bert_topic`: Function - Trains BERTopic using specified UMAP and HDBSCAN parameters and returns the topic model and coherence score.\n- `train_bert_model`: Function - Orchestrates grid search and saves the best BERTopic model."
}
